package com.example.app_pos.ui.dashboard.reports

import com.example.app_pos.R
import androidx.fragment.app.Fragment

/**
 * "Detaylı Bilgiler" tab — a placeholder for now.
 *
 * Passing the layout id to Fragment's constructor is the short form: the base
 * class inflates it, so there is no onCreateView to write. Enough while the
 * screen has no logic; it grows into the full form once date-range search and
 * the debt/collection totals land.
 */
class ReportsFragment : Fragment(R.layout.fragment_reports)
