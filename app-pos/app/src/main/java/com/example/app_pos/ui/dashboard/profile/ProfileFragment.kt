package com.example.app_pos.ui.dashboard.profile

import com.example.app_pos.R
import androidx.fragment.app.Fragment

/**
 * "Profil" tab — a placeholder for now.
 *
 * Will hold the merchant's phone and e-mail plus logout, which is what makes
 * the mock auth screen reachable again after a session ends.
 */
class ProfileFragment : Fragment(R.layout.fragment_profile)
